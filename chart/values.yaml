worldName: world-name
serverName: server-name
password: password
serverPublic: false

backupCron: "0 * * * *"
backupMaxAge: 3

resources: {}

replicas: 1

storage:
  kind: persistentVolumeClaim
  hostvol:
    path: /data/valheim
  pvc:
    size: 1Gi

networking:
  serviceType: NodePort

nodeSelector: {}
